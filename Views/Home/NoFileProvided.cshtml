@{
    ViewData["Title"] = "Error";
}

<div class="container-fluid">
    <div class="form-group row">
        <h1>No file name provided!!!</h1>
    </div>
    <div class="form-group row">
        <div class="col-sm-3">
            <button class="form-control btn btn-warning" onclick="@("window.location.href='" + @Url.Action("Index", "Home") + "'");">Home</button>
        </div>
    </div>
</div>
